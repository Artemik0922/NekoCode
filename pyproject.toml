[build-system]
requires = ["setuptools>=61.0", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "nekocode"
version = "0.6.0"
description = "NekoCode — Claude Code аналог: мульти-провайдер, скиллы, веб-инструменты, блюпринты, техдолг, экономия токенов, русский интерфейс"
requires-python = ">=3.10"
dependencies = [
    "rich>=13.0.0",
]

[project.scripts]
nekocode = "nekocode.cli:main"

[dependency-groups]
dev = [
  "pytest>=8.3.5",
]

[tool.setuptools]
packages = ["nekocode"]

[tool.pytest.ini_options]
testpaths = ["tests"]
